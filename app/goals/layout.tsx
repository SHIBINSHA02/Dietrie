// nextjs_deve/app/goals/layout.tsx
// nextjs_deve/app/dashboard/layout.tsx
import React from "react";

export default function GoalsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
